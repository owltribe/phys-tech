import { StyleSheet } from "react-native";

export const commonStyles = StyleSheet.create({
  container: {
    padding: 20,
    width: "100%",
    gap: 12,
    alignSelf: "center",
    justifyContent: "center"
  }
});
